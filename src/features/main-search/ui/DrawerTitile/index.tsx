import React from 'react';

import { SSearchInput } from './drawerTitle.styles';

const DrawerTitle = () => {
  return (
    <div>
      <SSearchInput placeholder={'Поиск'} />
    </div>
  );
};

export default DrawerTitle;
